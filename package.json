{
  "name": "libjskom",
  "version": "0.2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "private": true,
  "scripts": {
    "prepack": "tsc",
    "build": "tsc",
    "test:unit": "node --test test/unit/*.test.js",
    "test:e2e": "npm run build && npx vitest run --config e2e/vitest.config.ts",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "testcontainers": "^11.12.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  }
}
