{
  "name": "libjskom",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test:e2e": "bash test/e2e/run.sh",
    "docs:api": "npx jsdoc2md 'src/*.js' > docs/api.md",
    "docs:build": "npm run docs:api && uv run mkdocs build",
    "docs:serve": "npm run docs:api && uv run mkdocs serve"
  },
  "devDependencies": {
    "jsdoc-to-markdown": "^9.1.3"
  }
}
